crone:

2h fighting with crontab, so i decide i will shere it ;]
this nudle do the trick for me:

*/5 * * * * export DISPLAY=:0 && cd /path/to/your_project/directory && /path/to/.virtualenvs/name_of_the_env/bin/python your_script.py > /tmp/listener.log 2>&1

- run every 5 minutes
- the webdriver require display to be specified
- go to my proj directory. I'm writing results to a .csv "with open('rout.csv', 'a') as f:" and it's due to a path in 'open'
- run the script through a virtualenv
- (opt) write output to a file